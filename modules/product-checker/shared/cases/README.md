# Shared cases

Only cases with identical rules across every website belong here. Site-specific cases must remain inside `sites/<site-id>/cases/`.

Current shared candidates: duplicate alt text, missing alt text, and EI/AI synchronization once all sites expose equivalent fields.

