# Product fixtures

Save one sanitized HTML or API response per meaningful test scenario in a subfolder named after the website: `kfk`, `rfs`, `cfs`, or `rfk`.

Recommended minimum scenarios: normal variation product, printed product, product without socks, and a known failing case.

